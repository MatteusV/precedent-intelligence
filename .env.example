# Provision via Vercel Marketplace (Neon + Clerk), then:
# npm i -g vercel && vercel link && vercel env pull --yes

DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
DIRECT_URL="postgresql://user:pass@host/db?sslmode=require"  # optional; Neon sets DATABASE_URL_UNPOOLED

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

CURSOR_API_KEY="crsr_..."
JURISPRUDENCIAS_API_KEY="..."
